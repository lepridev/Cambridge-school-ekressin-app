import { Layout } from "@/ui/components/layout/layout";
import { ProjetPageContainer } from "@/ui/design-system/modules/projet/projet-page-view";
import React from "react";

type Props = {};

const Projet = ({}: Props) => {
  return (
    <Layout>
      <ProjetPageContainer />
    </Layout>
  );
};

export default Projet;
